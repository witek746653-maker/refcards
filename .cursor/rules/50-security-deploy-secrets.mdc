# 50 — Security & Deploy

- Never commit secrets (`SECRET_KEY`, tokens, credentials) — use env vars.
- Uploaded files go to `backend/uploads/` — validate extensions and size.
- Deployment should not expose admin-only scripts unless guarded by an explicit flag.
- Document any required env vars in `backend/env.example` or `README.md`.
